dependencies:
  pre:
    - gem install bundler --pre
    - gem update bundler
    - gem update rubocop

machine:
  timezone:
    Asia/Tokyo
  ruby:
    version:
      2.3.0

test:
  override:
    - bundle exec rake rubocop
